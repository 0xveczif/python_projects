We can use this program to scan devices in network to the ip we scan.

usage - sudo python2 network_scanner.py -t 10.0.2.16/24 

Requirements - install required libraries. For help, refer tools section
